# 生产环境配置

server:
  port: 82

spring:
  # 数据库设置
  datasource:
    url: jdbc:mysql://localhost:3306/zbl_missyou_v2?useSSL=false&serverTimezone=UTC&characterEncoding=UTF8
    username: "root"
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver